<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Padre sin ActionBar; el tema visual real (colores sénior) vive en Compose (ui/theme) -->
    <style name="Theme.CalculadoraInsumos" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
